task wrapper(type: Wrapper) {
    gradleVersion = '1.4'
}

task gitAdd(type:Exec) {
  commandLine 'git', 'add', 'nightly/'
}
task commitChanges(type:Exec) {
  commandLine 'git', 'commit', '-m', '"automatic updated of scaladoc"'
}
 
 
 buildscript {
    repositories {
        mavenCentral()
    }
}